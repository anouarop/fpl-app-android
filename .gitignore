*.iml
.gradle/
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
*.apk
*.aab
.env
.kotlin/
keystore.properties
*.jks
*.keystore
# backend/data.json is committed on purpose: it's the prebuilt manager index
# (537k managers) so the Render name-search service can serve without a Supabase
# Postgres connection (Render has no IPv6 egress to Supabase).
backend/server.log
backend/crawl.log

# Firebase service-account keys (secret) — never commit
firebase-*.json
google-services.json
